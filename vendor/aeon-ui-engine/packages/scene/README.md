# @aeon-ui/scene

Framework-agnostic CSS 3D scene helpers — perspective, `translate3d` / `rotateX|Y|Z` layers, wall clips.

Used by `@aeon-ui/react-scene` (`SceneStage`, `SceneLayer`, `ScenePlane`). See [SCENE.md](../../SCENE.md).
