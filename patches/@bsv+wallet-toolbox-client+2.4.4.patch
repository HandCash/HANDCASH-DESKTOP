diff --git a/node_modules/@bsv/wallet-toolbox-client/out/src/storage/methods/listOutputsIdb.js b/node_modules/@bsv/wallet-toolbox-client/out/src/storage/methods/listOutputsIdb.js
index 18b88ab..f4c3589 100644
--- a/node_modules/@bsv/wallet-toolbox-client/out/src/storage/methods/listOutputsIdb.js
+++ b/node_modules/@bsv/wallet-toolbox-client/out/src/storage/methods/listOutputsIdb.js
@@ -90,11 +90,13 @@ async function listOutputsIdb(storage, auth, vargs, originator) {
     }
     const includeSpent = false;
     const stati = ['completed', 'unproven', 'nosend', 'sending'];
+    // Omit undefined keys — StorageIdb/Knex reject `basketId: undefined`.
+    // Spec-op tag `all` clears basketId to review every basket (must omit, not pass undefined).
     const args = {
         partial: {
             userId,
-            basketId,
-            spendable: includeSpent ? undefined : true,
+            ...(basketId !== undefined ? { basketId } : {}),
+            ...(includeSpent ? {} : { spendable: true }),
             ...(specOp?.managedChangeOnly ? managedChange_1.managedChangeOutputFields : {})
         },
         txStatus: stati,
