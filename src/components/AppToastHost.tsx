/**
 * Floating toast host — intentionally empty.
 * Event toasts (payments, copy, errors) render in the BSV panel slot
 * so they keep the same footprint as the market ticker.
 */
export function AppToastHost() {
  return null
}
