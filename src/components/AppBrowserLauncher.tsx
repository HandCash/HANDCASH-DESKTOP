/**
 * BRC-100 apps open via Scan, deep links, or connected-app flows — not an
 * inline URL field on the Apps tab. The native in-app browser (mobile) is
 * still available through `window.handcash.openAppBrowser` when needed.
 */
export function AppBrowserLauncher() {
  return null
}
